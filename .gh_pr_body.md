## DATA-54: land DATA-52's payload onto the delivery line + DATA-28 per-item adjudication doc

Cherry-picks the 4 commits from the (recovered) `data-52` branch onto `agent/connectome-architect/data-43-merge-landing`, in the required order `01da3bd → af480cf → b3aec08 → 2c4f156`, plus a new per-item adjudication doc for every deletion `fcac719` performed against the DATA-28 server harness.

**Why cherry-pick, not merge the branch tip:** `data-52`'s baseline `a535633` is not an ancestor of the delivery line; `git diff landing..data-52` = 37 files +2905/−643, including pseudo-deletions that would roll back DATA-51's landed test changes. Merging the tip would silently lose that work. Cherry-picking keeps the deletion set to exactly the 55-line test-file move that is already adjudicated in the DATA-52 description.

Conflicts in the final commit (`2c4f156`) on `drososense/reservoir/runner.py` + `tests/test_reservoir_runner.py` (both already present via DATA-50's PR) were resolved by taking `2c4f156`'s version — `runner.py` ends up byte-identical to `2c4f156`'s blob (`2ea8289`…; sha256 `241ad561f6b1...`), and the `drososense/evaluation/runner.py` (DATA-51's skip-existing logic, a different file) is untouched.

New doc: `docs/data28_drop_adjudication.md` — file-by-file restore adjudication for `scripts/server/`, `tests/server/`, `results/server_runs/data28/` (all "restore"; no item was ever marked retired, and that empty "deferred" result is stated explicitly), plus why `connectome/select_neurons.py` and DATA-50's runner files are not `fcac719` deletions.

**Out of scope / not done, per the task's stated boundaries:** no changes to `configs/protocol_v1.1.yaml` / `v1.2.yaml`, no `m1_benchmark`/E1 regeneration, no edits to existing `results/raw/**` D2/D3 records, no schema migration introduced for tonight's E2 (E2 starts from `c2b3a8a`, matching D3's schema, and does not wait on this PR).

Evidence (commit shas, `git show --stat`, ancestor checks, blob-sha check, pytest summary) is in the landing comment on DATA-54.
